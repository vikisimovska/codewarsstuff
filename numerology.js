/**
 * Your solution
 * @date JS Date object
 **/
function solution(date){
  console.log(date);
  console.log(typeof date);
  // var lib = "1234567890";
  // var work = date.split('').filter(function(value){return (lib.indexOf(value) > -1)});
  // var result = 0;
  // work.map(function(value){
  //   result += Number(value);
  //   if (result > 10) {
  //     result = 1 + (result%10);
  //   }
  // })
  // return result;
}

// solution('06/14/2010')